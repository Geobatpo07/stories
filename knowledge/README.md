# Canonical knowledge objects

This directory contains the Knowledge Authoring Studio's canonical JSON records, grouped by
Kernel kind. Files are created automatically by Studio autosave and should be committed with
their generated MDX publications.

Do not hand-build records to bypass the Studio. If manual recovery is necessary, keep the
document envelope valid and run the full release verification afterward.

# lib/metadata

Reserved for shared `generateMetadata` helpers, Open Graph image generation, and
JSON-LD builders (`ScholarlyArticle`, `Dataset`, `SoftwareSourceCode` schema.org types —
see docs/roadmap). No implementation yet.

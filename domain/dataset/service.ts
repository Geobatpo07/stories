/**
 * DatasetService — intentionally empty this sprint.
 *
 * Future responsibilities (not implemented): list datasets, resolve by
 * slug, surface `Dataset` JSON-LD for open-science discoverability.
 */
export class DatasetService {}

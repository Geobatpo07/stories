/**
 * KnowledgeService — intentionally empty this sprint.
 *
 * Future responsibilities (not implemented): list/filter notes by
 * `noteType` or `programSlug`, resolve by slug, power the Research Threads
 * index described in the platform audit.
 */
export class KnowledgeService {}
